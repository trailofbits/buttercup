# Default values for competition-api chart
enabled: true
replicaCount: 1

service:
  port: 1323

resources:
  requests:
    cpu: 250m
    memory: 512Mi
  limits:
    cpu: 500m
    memory: 1Gi

dind:
  resources:
    requests:
      cpu: 250m
      memory: 512Mi
    limits:
      cpu: 500m
      memory: 2Gi

extraVolumeMounts:
  - name: tmp-storage
    mountPath: /tmp

nodeSelector: {}
tolerations: []
affinity: {}
