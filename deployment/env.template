# Buttercup CRS - Local Docker Compose Environment Configuration Template
# Copy this file to ../env.dev.compose and fill in the required values

# === Core Service Configuration ===

# CRS API settings
CRS_KEY_ID="515cc8a0-3019-4c9f-8c1c-72d0b54ae561"
CRS_KEY_TOKEN="VGuAC8axfOnFXKBB7irpNDOKcDjOlnyB"
CRS_KEY_TOKEN_HASH='$argon2id$v=19$m=65536,t=3,p=4$Dg1v6NPGTyXPoOPF4ozD5A$wa/85ttk17bBsIASSwdR/uGz5UKN/bZuu4wu+JIy1iA'

# === LLM Configuration ===

# LiteLLM Master Key (generate with: openssl rand -hex 16)
LITELLM_MASTER_KEY="d5179c62ae1c7366e3ee09775d0993d5"

# OpenAI API Configuration
OPENAI_API_KEY="<your-openai-api-key>"

# Anthropic API Configuration
ANTHROPIC_API_KEY="<your-anthropic-api-key>"

# Azure OpenAI (optional - leave commented out for offline mode)
# AZURE_API_BASE="<your-azure-api-base>"
# AZURE_API_KEY="<your-azure-api-key>"

# === Container Registry Configuration (optional for offline mode) ===

# GitHub Container Registry auth (optional - leave commented out for offline mode)
# Generate with: echo "USERNAME:$GHCR_PAT" | base64
# The PAT should have at least package:read permissions
# GHCR_AUTH="<your-ghcr-base64-auth>"

# Docker Hub credentials (optional, for authenticated pulls)
# DOCKER_USERNAME="<your-docker-username>"
# DOCKER_PAT="<your-docker-pat>"

# Container image organization for challenges
# For offline mode, use local registry or build images locally
FUZZ_TOOLING_CONTAINER_ORG="local/oss-fuzz"

# === Optional Services ===

# === Optional Cloud Monitoring Services (disabled by default) ===

# LangFuse telemetry (optional - leave commented out for offline mode)
# LANGFUSE_ENABLED=true
# LANGFUSE_HOST="<your-langfuse-host-url>"
# LANGFUSE_PUBLIC_KEY="<your-langfuse-public-key>"
# LANGFUSE_SECRET_KEY="<your-langfuse-secret-key>"

# OpenTelemetry endpoint (optional - leave commented out for offline mode)
# OTEL_EXPORTER_OTLP_ENDPOINT="<your-otel-endpoint>"
# OTEL_EXPORTER_OTLP_HEADERS="Authorization=Bearer <your-token>"
# OTEL_EXPORTER_OTLP_PROTOCOL=grpc

# === Competition API Configuration (optional) ===

# Enable mock competition API for local testing
MOCK_COMPETITION_API_ENABLED=false

# External competition API settings (if connecting to a real competition)
# COMPETITION_API_ENABLED=false
# COMPETITION_API_KEY_ID="11111111-1111-1111-1111-111111111111"
# COMPETITION_API_KEY_TOKEN="secret"
# COMPETITION_API_URL="<competition-api-url>"

# === Local Development Settings ===

# Base image for fuzzer containers
FUZZER_BASE_IMAGE="gcr.io/oss-fuzz-base/base-runner"

# Redis URL (default for local deployment)
REDIS_URL="redis://redis:6379"

# Working directories (mounted as volumes in compose.yaml)
CRS_SCRATCH_DIR="/crs_scratch"
TASKS_STORAGE_DIR="/tasks_storage"
NODE_DATA_DIR="/node_data"