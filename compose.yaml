services:
  orchestrator:
    build:
      context: ./orchestrator
      target: development
    volumes:
      - ./orchestrator:/app
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
